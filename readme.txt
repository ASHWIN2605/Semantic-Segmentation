Semantic Segmentation is an image analysis task in which we classify each pixel in the image into a class.

This code classifies the image into 21 different classes.

Download the jupyter file and place the image needs to be segmented and call the image file in the code to segment it.